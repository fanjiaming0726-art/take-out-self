（一）ai
在苍穹外卖项目中引入AI agent功能参考美团的小美APP，并把整个项目打包成一个安卓软件，实现真正的仿美团点餐系统


（二）redis
Redis黑名单、RabbitMQ、Redis Zet、MangoDB
PostgreSQL、Lua、互斥锁、三级语义原子切分、
sentinel接口限流、
熔断与降级、Elastic Search做商品检索优化、数据一致性
乐观锁
持久机制，Redis数据结构


（三）RabbitMQ/RocketMQ
异步削峰


（四）mysql
事务，锁机制，MVCC，日志

（五）Gateway网关
断点续传

（六）spring
引入AOP

（四）代办：
1. 跑通接口 
2. 几乎所有的操作都需要登录凭证，不能每一次都要写if(id == null) return。所以务必写一个统一登录拦截器 
3. 实现redis和rocket、mq功能 
4. 深入底层了解LambdaQueryMapper baseMapper ServiceImpl的底层实现，明白其为什么可以自动生成sql语句，会怎么去生成sql语句。（√）
5. 将逻辑封装在实现类里，controller层只调用接口服务，移除ServiceImpl这个东西